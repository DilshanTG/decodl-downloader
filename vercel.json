{
  "buildCommand": "npm install -g @wasp.sh/wasp-cli@0.22.0 && cd app && REACT_APP_API_URL=https://api.stockmart.lk wasp build && npm install && REACT_APP_API_URL=https://api.stockmart.lk npx vite build",
  "outputDirectory": "app/.wasp/out/web-app/build",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Origin", "value": "*" }
      ]
    }
  ]
}
